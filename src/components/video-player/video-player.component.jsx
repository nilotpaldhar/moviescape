import React from 'react';
import ReactPlayer from 'react-player/youtube';

import './video-player.styles.scss';

const VideoPlayer = () => {
	return <ReactPlayer url='' controls={true} width='100%' />;
};

export default VideoPlayer;
